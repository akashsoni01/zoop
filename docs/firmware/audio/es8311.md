# audio/es8311.rs

- **Path:** `firmware/src/audio/es8311.rs`
- **Purpose:** ES8311 + I2S audio BSP stub implementing `Audio`. Logs init; record/play methods are no-ops/errors until `esp_codec_dev` is wired.

## Component in architecture

```mermaid
flowchart LR
  ENG["FirmwareEngine"]
  ES["Es8311Audio HIL stub"]
  I2S["I2S pins from config"]
  ENG --> ES -.-> I2S
  style ES fill:#f96,stroke:#333,stroke-width:3px
```

## Responsibilities

Stub `Audio` trait for compile/link; real capture/playback pending HIL.

## Key types / functions

| Item | Role |
|------|------|
| `Es8311Audio::init` | Log + return stub |
| `Audio` impl | `set_volume`, `play_beep`, record/playback stubs (`read_record_chunk` → 0) |

## Constants / formats

Uses `board::config` I2S pins and `SAMPLE_RATE` 16 kHz.

## Dependencies

`zoop_core::io::Audio`, board config. Inbound: `main`, engine.

## Status

HIL stub.

## Related

[../../core/record.md](../../core/record.md), [../board/config.md](../board/config.md), [../../architecture.md](../../architecture.md)
