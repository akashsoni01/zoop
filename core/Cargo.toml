[package]
name = "zoop-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Host-testable core logic for Zoop (storage, WAV, battery, state)"

[dependencies]
thiserror = "2"
tempfile = "3"

[dev-dependencies]
