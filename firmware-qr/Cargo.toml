[package]
name = "zoop-firmware-qr"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Host demo for Zoop QR scan → string (no esp-idf yet)"

[[bin]]
name = "zoop-firmware-qr"
path = "src/main.rs"

[dependencies]
zoop-core = { path = "../core" }
qrcode = { version = "0.14", default-features = false }
